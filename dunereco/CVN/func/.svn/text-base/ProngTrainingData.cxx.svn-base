////////////////////////////////////////////////////////////////////////
// \file    ProngTrainingData.h
/// \brief   The ProngTrainingData object contains a PixelMap and the
///          output class type, and any other bit that goes into the ANN
// \author   psihas -- psihas@fnal.gov

//#include "art/Framework/Services/Registry/ServiceHandle.h"

#include "CVN/func/ProngTrainingData.h"
#include "CVN/func/ProngType.h"

namespace cvn
{

  ProngTrainingData::ProngTrainingData(const ProngType& pronglabel,
                    const ProngType& pronglabelX, const ProngType& pronglabelY,
    		    double purity3D, double purityX, double purityY,
		    double recE, unsigned int ncellX, unsigned int ncellY,
                    double vX, double vY, double vZ, const PixelMap& pMap):
    fLabel3D(pronglabel),
    fLabelX(pronglabelX),
    fLabelY(pronglabelY),
    fPurity3D(purity3D),
    fPurityX(purityX),
    fPurityY(purityY),
    fRecE(recE),
    fNCellX(ncellX),
    fNCellY(ncellY),
    fVertX(vX),
    fVertY(vY),
    fVertZ(vZ),
    fProngPMap(pMap)
  {  }

} // end namespace cvn
////////////////////////////////////////////////////////////////////////
